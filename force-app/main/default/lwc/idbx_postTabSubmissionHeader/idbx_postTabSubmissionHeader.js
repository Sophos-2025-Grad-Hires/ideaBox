import { LightningElement } from 'lwc';
import headerImage from '@salesforce/resourceUrl/idbx_postTabSubmissionHeader';

export default class PostTabHeader extends LightningElement {
    headerImage = headerImage;
}